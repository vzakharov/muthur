Proposed squash title/body:

```
feat: record the operator's GitHub handle in each cost row (pr #104)
```

```
The cost ledger could say what a session cost and which branch spent
it, but not whose session it was, so spend could not be split by
person.

Each row under .claude/costs/sessions/ now carries `operator`: the
lowercased handle .claude/hooks/operator-voice.sh resolved at startup,
read back out of the transcript from that hook's own SessionStart
record. It stays null when the hook named nobody (no gh, or a bot's
token) rather than being guessed from the pusher, who may be the
agent's own account. The hook's comment marks its wording as parsed.

report.py sums the rows by operator as well, filing a row without one
under "(unknown)". The rows already in the ledger, whose transcripts
are gone, are backfilled by hand as @vzakharov's.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
