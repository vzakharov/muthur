Proposed squash title/body:

```
fix: /handle reports the run, not the flag nobody typed (pr #85)
```

```
`/handle`'s rule is that every flag fires on presence, the reporting
included: what a flag would have done had it been typed is a rule to
apply, never a finding for the operator to read. One carve-out
survived it. Absent `and finalize`, Step 5 still had the turn end
with a note that land-prep was not requested — a ruling on an
argument the operator did not write, and in practice a paragraph
explaining why `/finalize` is opt-in and how to invoke it, addressed
to someone who had just typed `/handle` and knew both.

Step 5 now keeps only why the flag is opt-in, and ends on the lane's
own report. The presence rule at § "Argument shape" names the
`and finalize` case beside the `and merge` one, so a reader meets the
rule once rather than meeting its exception at the step that used to
carry it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
