Proposed squash title/body:

```
feat: have /plainly ground every answer in a minimal example (pr #93)
```

```
A reader who lost a general statement usually follows a single case,
and "give me a concrete example" is the follow-up they most often ask.
/plainly now answers it up front: whatever the answer is about — a
rule, a design choice, a fix — the pass puts one concrete case of it
after the causal chain. For a fix, that is the scenario that broke.

The example is the smallest instance that still shows the point, and
may be invented rather than lifted from the codebase when a toy case
shows the shape more clearly. It follows the conclusion and never
replaces it. The only answer exempt is one already told as a scenario
the reader could replay; the six defects are unchanged.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
