Proposed squash title/body:

```
feat: route untracked work to /task, and carve in /plan (pr #74)
```

```
The routing ladder sent every new session to /plan unless the prompt
matched one of four named exceptions, so a question formally got a plan
file and no agent ever wrote one — the ladder was already overridden by
judgment it did not state. It now reads whether the prompt asks for a
change to the codebase: a change routes to /task, a change carrying an
issue number to /issue, and a prompt asking for none is answered in chat
under no skill at all. The operator's gate is not removed but moved,
since /task Question 1 routes gate-worthy work back to /plan — it fires
when the questions say so rather than on every session.

The decision to carve a task into several PRs moves out of /issue and
into /plan. /issue made that call before anything had read the code, and
confined carving to work that already carried an issue number; it is now
transport — export, commit, hand over — the shape /pr has. /plan makes
the call, specs the first slice in full, describes the rest coarsely,
and files the slices, creating the parent umbrella itself when the work
arrived untracked. The first slice is always a child issue and never the
parent, which stays open as the grouping artifact.

Two constraints fix where that work lands. /pr reads the closing issue
off the branch slug and /plan renames the branch before writing the plan
file, so the slices are filed inside the plan turn rather than deferred
to /go, behind the propose-and-stop gate /issue used. And /plan is G2
while /issue is G3, so the carve splits on that line: the decision and
the plan-file shape in /plan, the filing mechanics in
.claude/skills/issue/splitting.md, cited conditionally so an adopter
running without a tracker still gets the half that works.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
