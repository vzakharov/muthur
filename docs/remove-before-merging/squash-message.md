Proposed squash title/body:

```
feat: #69 colocate three minority-entry skill sections (pr #80)
```

```
A skill loads as one unit, so a section that fires on a single
invocation form is paid for by every run of that skill. #68 set the
shape by moving /plan's plan-or-go decision beside it; this applies the
same three-part criterion — a trigger a pointer can state, a minority
trigger, a resident half that survives the cut — across the skills the
loop loads on every session.

Three sections move to colocated sidecars, each behind a resident
pointer that names its trigger: /plan's native plan-mode recovery, the
branch of /squash-message's working-file selection that runs only when
neither file is on disk, and /take-issue's frame-extraction procedure
for a video attachment. The plan-mode hook now cites its sidecar by
path rather than naming a section by heading, which puts that citation
under the cross-reference check instead of leaving it to break
silently on a reword.

Four candidate sites are declined with the reason recorded, since a
rejection is the same finding as an extraction: /go's canary and
/from-branch's two sections are not minority entries, /tend-prose is
inverted because its default sweep needs all four lenses, and /issue's
split machinery is already colocated under /plan. check-skill-catalog.sh
gains the reverse of its existing reference check — a sidecar nothing
points at fails as silently as a pointer to nothing, and this sweep
multiplies both.

Closes #69

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
