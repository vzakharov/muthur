Proposed squash title/body:

```
feat: have /plainly show a minimal example when the point is abstract (pr #93)
```

```
A reader who lost a general statement usually follows a single case,
and "give me a concrete example" is the follow-up they most often ask.
/plainly now answers it up front: when the point is a rule, a mechanism
or a distinction, the pass puts one example after the causal chain.

The example is the smallest instance that still shows the point, and
may be invented rather than lifted from the codebase when a toy case
shows the shape more clearly. It follows the conclusion and never
replaces it; an answer that is already a particular needs none. The six
defects are unchanged, since a missing example is owed only where the
point is abstract.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
