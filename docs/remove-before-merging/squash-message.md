Proposed squash title/body:

```
feat: context budget measures "nearly done" as under 100k tokens (pr #101)
```

```
The context budget notices let an agent finish instead of stopping when
the work is nearly done, and defined that as "a small step or a commit
or two from finished". Agents read it as a count of edits, which says
nothing about whether the rest of the work fits in the context left.

Both notices now ask for the agent's own estimate that finishing takes
under ~100k more tokens of context, so a session at the 200k warning
that expects to wrap up before ~300k carries on. The warning notice
adds a rough gauge: less than half of what the session has already
carried. The pause notice keeps the number alone, since at 300k that
gauge would read 150k; a test pins which notice carries it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
