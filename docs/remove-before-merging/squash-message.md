Proposed squash title/body:

```
fix: make /handle dispatch to /go imperatively (pr #76)
```

```
/handle described where its lanes land instead of instructing the
agent to load /go, so runs did the lane's work inline and never
reached that skill's mandatory /dry and /tend-prose passes. The
trailing assurance that the passes "come along either way" made it
worse: it reads as a receipt for a run nobody performed.

Step 3 now issues the imperative every other dispatch site in the
tree uses, and states the completion condition instead of promising
it — neither the plan lane nor the review lane is finished until
/go's quality passes and its hand-off to /pr have run.

The skill's Do NOT clause bound only references phrased "load and
follow", which by its own wording exempted the one reference that
wasn't. It now binds every skill reference whatever verb introduces
it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
