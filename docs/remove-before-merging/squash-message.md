Proposed squash title/body:

```
feat: export a launch prompt's issue before the first turn (pr #75)
```

```
A session launched with an issue title and a `#<N>` tail spent its
first turn doing what the machine could have done before it started:
recognising the tail, loading `/issue`, and running that skill's Step 1
export. The round trip bought nothing that could not be bought earlier.

A UserPromptSubmit hook now runs the export ahead of the agent's first
token and names the written path in the turn's context, so the run
starts at Step 2. Two gates guard it, both failing closed: the prompt
is the session's first — exact, because the prompt being submitted is
not in the transcript yet — and it names an item, as a trailing `#<N>`,
a full issue or PR URL, or an explicit `/issue` invocation. Every
uncertainty declines, since a wrong decline costs only the status quo
while a wrong fire writes stray exports on every prompt of every
session.

The routing convention and the commit stay where they were. The agent
still reads the tail as an `/issue` invocation, which is what covers
the prompts the hook declines and the harnesses that run no hooks, and
`/issue` Step 2 still commits the export once the branch is settled —
a hook that committed would commit to whatever HEAD happened to be on.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
